const current = document.querySelector('#current');
const imgs = document.querySelectorAll('.grid-images');


imgs.forEach(img => img.addEventListener('click', () => (current.style.backgroundImage = e.stlye.backgroundImage)))
